<!DOCTYPE html>

<html lang="en">
{% include 'head.html' %}
<body>
    <div id="container">
        <a class="header" href="."><h1>IndieCert</h1></a>

        {% if redirect_to %}
            <p class="warn">
                You tried to access <code>{{ redirect_to|e }}</code> which requires
                authentication.
            </p>
        {% endif %}

        <p>
            Please provide your domain name.
        </p>

        <form class="authenticate" method="post" action="_indieauth/auth">
            <input required type="text" name="me" placeholder="yourdomain.com">
            <input type="hidden" name="redirect_to" value="{{ redirect_to|e }}">
            <input class="info" type="submit" value="Sign In">
        </form>

        {% include 'footer.html' %}
    </div> <!-- /container -->
</body>
</html>
